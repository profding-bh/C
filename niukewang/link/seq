#define MaxSize 1000
ElemType A[MaxSize];
int n;


ERRORMESSAGE("");

void insertlist(ElemType A[],int &n, int i,ElemType item)
{
	int j;
	if(n == MaxSize || i < 1 || i > n+1)
	{
		ERRORMESSAGE("表满或插入位置不正确")；
	}
	for(j=n-1;j>=i-1;j--)
		A[j+1]=A[j];
	A[i-1]=item;
	n++;
}





